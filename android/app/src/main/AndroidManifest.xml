<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SDL2 requires write permission for save files on older devices -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Vibration for controller feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Require OpenGL ES 2.0 -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.SMC"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:appCategory="game">

        <!-- Android 16 (API 36) ignores android:screenOrientation on displays
             at or above 600dp smallest width. Measured on an API 36 emulator
             resized to 1600x2560 @320dpi (800dp): the landscape lock was
             dropped and the game rendered its scene into a band at the top of
             a portrait window, leaving two thirds of the screen empty.
             appCategory="game" alone did not exempt it. This property is the
             documented opt-out; it is transitional and Google will retire it,
             so the engine's portrait fallback stays in place behind it. -->
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_ORIENTATION"
            android:value="true" />

        <!-- Use native GLES driver instead of ANGLE to avoid EGL pre-rotation
             issues on Android 15+ / Pixel Fold (ANGLE requests buffer pre-rotation
             transforms that SDL2 2.28 does not handle, causing BLASTBufferQueue
             rejections and SIGABRT). -->
        <meta-data android:name="android.app.angle.driver" android:value="native" />

        <activity
            android:name=".SMCActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SMC.Splash"
            android:screenOrientation="landscape"
            android:hardwareAccelerated="false"
            android:windowLayoutInDisplayCutoutMode="shortEdges"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|density|uiMode"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
